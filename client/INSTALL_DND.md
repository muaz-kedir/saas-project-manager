# Install Drag & Drop Dependencies

Run this command in the `client` directory:

```bash
cd client
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

After installation, restart your frontend dev server:

```bash
npm run dev
```

Then the drag & drop functionality will work!
